from base import Base

class Default(Base):
	def help(self):
		return """Sorry we couldn't figure out the command you were trying to send us \n
		To see a list of commands for modules text us back with yelp help, maps help, or trip help"""